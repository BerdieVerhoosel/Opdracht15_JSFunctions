

const paintWalls = function (wallType, paintColor) {
    console.log("The " + wallType + " wall has been painted " + paintColor);
};

const color = ("green");
const wall = ("South-east");

paintWalls(wall, color);

//6. Yes the order of the arguments matter.
//7. The arguments are logged in a different order.
//8. Then it's allreight again.
